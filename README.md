# Token Wallet DApp

## Setup
1. Compile & deploy your smart contract.
2. Paste ABI in `src/TokenWalletABI.json`.
3. Replace `YOUR_CONTRACT_ADDRESS_HERE` in `App.jsx`.
4. Run `npm install` & `npm run dev` to test locally.
5. Push to GitHub & connect repo to Vercel for deployment.